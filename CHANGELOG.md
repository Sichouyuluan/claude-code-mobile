# Changelog

## v2.0.0 (2026-05-24)

### 新增
- **终端模式**：WebSocket 实时流式传输 Claude CLI 完整输出到网页（思考过程、工具调用、代码回复）
- **双模式切换**：对话模式 + 终端模式一键切换，浮动按钮操控
- **面板重写**：PyWebView + Liquid Glass UI 管理面板，替代原 CustomTkinter
- **自动模型匹配**：发消息时自动检测会话使用的模型，避免模型切换报错
- **修复 cwd 传递**：发消息时正确传递工作目录，解决静默失败问题
- **移动端适配**：增大 safe-area padding，防止 Android 浏览器工具栏遮挡输入框

---

## v1.1.0

### 改进
- 侧边栏工作区下拉箭头加大
- 面板重设计方案 B（PyWebView）详细规划文档

---

## v1.0.0

### 新增
- **全面日志系统**：关键操作全部输出到面板日志（RotatingFileHandler + PinHidingFilter）
- **thinking 内容提取**：从 JSONL 中提取 Claude 的思考过程并渲染
- **tool_result 内容提取**：显示工具执行结果

### 修复
- 日志系统初始化：`setup_logger()` 未被调用导致日志始终为空
- 日志面板重写：读取所有日志文件，不依赖服务器状态

---

## v0.9.0

### 新增
- **分层渲染**：按轮次折叠消息，2000+ 条消息性能优化
- 轮次改为分隔线，消息独立显示（不再塞进同一容器）

### 修复
- thinking/tool_result 内容提取完善

---

## v0.8.0

### 新增
- **全量加载消息**：移除分页，一次加载所有消息
- **滚动条用户消息标记**：蓝色圆点标记用户消息位置
- 工具调用详情折叠显示 + 消息合并修复 + 时间戳右置

### 修复
- 活跃会话点击直接进入对话
- 侧边栏/会话列表改进

---

## v0.7.0

### 新增
- **对话流合并**：tool_result 用户消息合并到 assistant 组
- **代码复制按钮**：代码块右上角一键复制
- **滚动加载**：上滑自动加载更多消息
- 对话页顶部显示完整会话 ID + 复制按钮
- XSS 防护（safeUrl 过滤 javascript:/data: 协议）

### 修复
- 前端消息重复、分页失效等 bug
- 写操作限速 + 面板密钥显示修复
- 安全漏洞修复（路径穿越、上传大小限制）

---

## v0.6.0

### 新增
- **静默启动**：CCM.pyw + 启动.vbs 无命令行窗口启动面板
- **权限弹窗提示**：显示 CC 请求权限警告 + 工具名 + 操作指引
- 面板关闭/启动时按端口杀残留进程 + atexit 兜底

### 修复
- 窗口闪烁彻底消除
- BAT 改为调用 VBS，零窗口
- 状态栏图标高清化（11 尺寸 ICO）

---

## v0.5.0

### 新增
- **SSE 流式输出**：实时渲染 + 打字机效果
- **液态玻璃 + 拟态 UI**：backdrop-filter blur + 内外阴影
- 面板添加修改密钥按钮
- 密钥修改直接写文件，不需要服务器运行

### 修复
- 密钥输入框编辑时不会被自动刷新覆盖
- 保存密钥前检查服务是否运行

---

## v0.4.0

### 新增
- **面板重构**：液态玻璃 UI + 修复崩溃 + 新增功能
- 重写面板为 CustomTkinter 原生桌面面板
- 9/9 测试用例全部通过

### 修复
- 放宽限速策略防止误封
- 工作区路径解码 + 面板崩溃修复

---

## v0.3.0

### 新增
- **PyWebView 桌面管理面板**：服务器启停、连接信息、系统状态、日志查看
- 全中文界面 + 密钥/链接一键复制
- 会话卡片优化

### 修复
- 工作区识别和面板启动
- BAT 文件编码修复（GBK 兼容）

---

## v0.2.0

### 新增
- **安全框架**：速率限制、扫描攻击检测、设备追踪、中间件
- **数据读取器**：读取 ~/.claude/ 目录的 JSONL 会话数据
- **消息发送器**：通过 CLI --resume 注入消息
- **API 路由**：认证、数据 API、页面服务
- **前端 SPA**：响应式暗色主题、仪表盘、会话查看器
- 全中文界面

---

## v0.1.0

### 新增
- 项目初始化：基础结构、配置文件、入口点
